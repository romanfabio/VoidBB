<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= TITLE %></title>
    <link rel="stylesheet" href="/stylesheets/default.css">
    <% if(locals.styles) { %>
        <% for(let i = 0; i < styles.length; i++) { %>
            <link rel="stylesheet" href="/stylesheets/<%= styles[i] %>">
        <% } %>
    <% } %>
</head>
<body>
    <div id="page">
        <header>
            <a href="/"><img id="header_logo" src="/images/logo.jpeg"></a>
        </header>
        <nav>
            <ul id="menu">
                <li class="menu-item left"><a href="/">Home</a></li>
                <% if(!locals.USERNAME) { %>
                <% if(locals.can_register) { %>
                <li class="menu-item right"><a href="/register">Register</a></li>
                <% } %>
                <li class="menu-item right"><a href="/login">Login</a></li>
                <% } else { %>
                    <li class="menu-item left"><a href="/newtopic">New Topic</a></li>
                <li class="menu-item right"><a href="/logout">Logout</a></li>
                <li class="menu-item right">Hello, <%= USERNAME %></li>
                <% } %>
            </ul>
        </nav>
        <main>
            <% if( locals.ERROR ) { %>
            <div id="error-div">
                <h5 id="error"><%= ERROR %></h5>
            </div>
            <% } %>
            <%- body %>
        </main>
        <footer>
            <a href="https://github.com/romanfabio/VoidBB" target="_blank"><span id="copyright">&copy; VoidBB</span></a>
        </footer>
    </div>
</body>
</html>