PostgreSQL Database
    DB name => voidbb
    user    => voidbbuser
    pass    => voidbbuser
    port    => 5432

!!! All the required tables are autogenerated if they don't already exists !!!


"Variables" table, required entities:

 key                          |     value     | isInt 
------------------------------+---------------+-------
 FORUM_NAME                   | Cipolla Spawn | f
 FOUNDER_NAME                 | mrvoid        | f
 FOUNDER_EMAIL                |               | f
 PASSWORD_MIN_LENGTH          | 8             | t
 PASSWORD_MAX_LENGTH          | 64            | t
 USERNAME_MIN_LENGTH          | 4             | t
 FORUM_DESCRIPTION_MAX_LENGTH | 255           | t
 POST_TITLE_MAX_LENGTH        | 255           | t
 USERNAME_MAX_LENGTH          | 32            | t
 FORUM_NAME_MAX_LENGTH        | 32            | t


1) Install NodeJS required packages

npm install

2) Generate a secret key for session's encryption:

./node_modules/.bin/secure-session-gen-key > secret-key

3) Run

npm start