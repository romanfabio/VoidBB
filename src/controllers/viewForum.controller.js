const db = require('../database/db');
const viewer = require('../util/viewer');

module.exports = {
    get: (request, reply) => {
        //Controlla che request.params.name non sia stringa vuota
    }
}